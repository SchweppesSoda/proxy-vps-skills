interface:
  display_name: "Proxy Config Audit"
  short_description: "Read-only audit for five-client config and writer drift."
  default_prompt: "Use $proxy-config-consistency-audit to audit five-client config, generated-writer ownership, and CustomRules drift."

policy:
  allow_implicit_invocation: true
