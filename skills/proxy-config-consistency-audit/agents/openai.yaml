interface:
  display_name: "Proxy Config Audit"
  short_description: "Inspect cross-client or generated config drift."
  default_prompt: "Use $proxy-config-consistency-audit to inspect the requested consistency scope and report findings."

policy:
  allow_implicit_invocation: true
