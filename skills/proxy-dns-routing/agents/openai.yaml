interface:
  display_name: "Proxy DNS Routing"
  short_description: "Maintain resolvers and node/domain DNS mappings."
  default_prompt: "Use $proxy-dns-routing to update the requested DNS mapping and verify its resolution path."

policy:
  allow_implicit_invocation: true
