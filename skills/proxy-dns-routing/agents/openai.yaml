interface:
  display_name: "Proxy DNS Routing"
  short_description: "Maintain five-client DNS projections and AirportServers."
  default_prompt: "Use $proxy-dns-routing to update DNS semantics or AirportServers with an audit and generated handoff when needed."

policy:
  allow_implicit_invocation: true
