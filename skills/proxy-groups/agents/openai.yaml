interface:
  display_name: "Proxy Group Maintenance"
  short_description: "Maintain provider identities and group membership."
  default_prompt: "Use $proxy-groups to update the requested provider or group and verify its affected references."

policy:
  allow_implicit_invocation: true
