interface:
  display_name: "Proxy Rule Policy Routing"
  short_description: "Maintain rule precedence and policy mappings."
  default_prompt: "Use $proxy-rule-policy-routing to update the requested rules and check affected targets and order."

policy:
  allow_implicit_invocation: true
