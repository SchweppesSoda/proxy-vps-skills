interface:
  display_name: "Proxy Rule Policy Routing"
  short_description: "Maintain cross-client rule order and policy routing safely."
  default_prompt: "Use $proxy-rule-policy-routing to add, remove, or remap service rules with a policy audit first."

policy:
  allow_implicit_invocation: true
