interface:
  display_name: "Proxy Rule Policy Routing"
  short_description: "Maintain rule order, providers, and policy targets safely."
  default_prompt: "Use $proxy-rule-policy-routing to update rule policy routing or CustomRules consumers while preserving canonical order."

policy:
  allow_implicit_invocation: true
