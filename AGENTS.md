# proxy-vps-skills maintenance

本仓只维护 `skills/` 的工作流、引用文档和审计工具；配置、provider 数据及生成器实现归各业务仓库。

## 本地工作与完成标准

- 在主目录 `main` 自主完成范围内的编辑、可逆本地生成、相关验证和提交，不在第一版后等待 review。同仓一个写入任务；仅用户要求隔离/并行时另建工作树，完成后按内容收回并清理。
- 开始核对仓库、分支、工作树、已有改动和上游。保留无关改动；需要刷新上游便 fetch，离线注明未核实并继续独立本地工作。安全时 fast-forward，分叉不重置/强推；发布前再确认目标。
- 搜索和测试从变更对象与直接消费者开始；共享契约、重命名/删除或失败证据才扩大。文档检查链接/diff，行为修改跑对应测试，通过后不无理由重复；正式发布门禁保留。
- 完成包括请求落地、相关验证/文档和范围明确的本地提交；报告限制、本地/远程及发布状态。推送、Release、部署沿用会话授权；缺少授权时先准备可审阅的本地结果，仅暂停依赖该授权的动作。
- 临时输出用 `.tmp/`；清理前核对未跟踪、忽略文件及恢复依赖，必要备份放仓库外。保留历史 tag/冻结归档，不整目录忽略共享 `.codex/` 配置。

## Skill 修改

- description 只写真实能力和判别边界；不堆 provider/service 关键词。入口保留路由与关键合同，操作细节放已有 references，按任务读取。
- 审查入口与引用中冲突的强制流程；保留实际 ownership、凭据、事务和用户设置边界。不要把一次事故推广成每次任务的全局门禁。
- 文档修改检查 frontmatter、链接、路由场景和 diff；脚本逻辑变化运行对应测试。无脚本变更无需执行全部配置审计或测试套件。
- 只同步改动的个人安装副本，先辨认本地差异，保留既有 policy/dependencies；不要改第三方插件缓存。核验源码/安装副本一致并报告同步状态。
- 仓库入口见 [README.md](README.md)。可复用 skill 不含个人盘符、provider capability URL 或配置快照。
